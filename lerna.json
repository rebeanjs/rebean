{
  "packages": ["packages/*"],
  "version": "independent",
  "npmClient": "npm",
  "command": {
    "publish": {
      "allowBranch": "master",
      "ignoreChanges": ["*.md"],
      "message": "chore(release): publish"
    }
  }
}
