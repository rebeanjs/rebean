{
  "packages": ["packages/*"],
  "version": "independent",
  "npmClient": "yarn",
  "command": {
    "publish": {
      "allowBranch": "master",
      "ignoreChanges": ["*.md"],
      "message": "chore(release): publish"
    }
  }
}
